/**
 * Test-only stub for `ajv-formats`. See sibling `ajv.mjs` stub for rationale.
 */
export default function addFormatsStub() {
  // No-op: called only if the real Ajv validator were constructed.
}
