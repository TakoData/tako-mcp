# Exa highlights vs page text — web snippet A/B

GENERATED by `scripts/evals/web-snippet/report.ts`. Measurement, not a gate.

Source: `raw-2026-07-31-full.jsonl` + judged
Cases: 26 · snippet_max_chars: 2000

## Run health

Read this before anything else. A failed or rate-limited arm reaches the
caller as an empty result set, not as an error — so a broken run looks like
a coverage finding unless the failures are counted here first.

| endpoint | arm | calls | non-200 | zero-result | median results |
|---|---|---:|---:|---:|---:|
| search | text | 26 | 0 | 0 | 10.0 |
| search | highlights | 26 | 0 | 0 | 10.0 |
| answer | text | 26 | 0 | 0 | 3.0 |
| answer | highlights | 26 | 0 | 0 | 3.0 |

## Latency — the answer gate, and the search cost

Exa selects highlights with an LLM, so they cost time even though they cost
no money. The two endpoints turn that cost into different questions and both
rows below want reading:

- **answer** has a cliff. `GROUNDING_WEB_RETRIEVAL_TIMEOUT_S` is 1.5s and a
  breach does not return a slow answer — it returns no web grounding at all.
  So the max column decides that one, not the median.
- **search** has no cliff, so nothing here can fail; the paired median is
  simply a price. `tako_search` is the higher-volume tool and defaults to
  `count: 10`, so that price is paid far more often than the answer one —
  read the search row against the byte and utility gains, not past it.

Arm order is rotated per case, so neither arm systematically eats the cold cache.

| endpoint | arm | median ms | max ms | n |
|---|---|---:|---:|---:|
| search | text | 750 | 1368 | 26 |
| search | highlights | 836 | 1237 | 26 |
| answer | text | 1738 | 2672 | 26 |
| answer | highlights | 1694 | 8585 | 26 |

**Timed to response headers, not to a complete body.** `fetch` resolves at headers, so these numbers exclude body download — a component systematically larger on the text arm, whose payload is ~18% bigger. Every `highlights − text` delta below is therefore an **upper bound**: the true gap is smaller than shown. Sweeps collected after this was fixed are timed to the complete response and say so here.

**Web-grounding survival on /v1/answer** — the actual degradation mode. A
breach of the 1.5s retrieval budget does not return a slow answer; it returns
an answer with no web grounding at all. So a 200 with an empty `web_results`
on a web-only request is the failure this table counts, and it is a stronger
signal than wall-clock latency (which also includes arbiter synthesis).

| arm | answer calls | zero web results | rate |
|---|---:|---:|---:|
| text | 26 | 0 | 0% |
| highlights | 26 | 0 | 0% |

**Paired per-case delta** (highlights − text on the same query — the only
comparison that cancels query difficulty):

| endpoint | median Δms | max Δms | slower on |
|---|---:|---:|---|
| search | 49 | 522 | 18/26 cases |
| answer | 1 | 6111 | 13/26 cases |

## Coverage — null snippets and cap binding

With highlights on, Tako requests no page text, so a page with no relevant
passage returns `snippet: null` and keeps its slot. No text fallback was
added upstream, on the basis of 0 nulls across ~40 results. This is the
number that would overturn that.

| arm | results | null snippets | at cap | median chars | mean chars | contains ` … ` |
|---|---:|---:|---:|---:|---:|---:|
| text | 260 | 0 | 167 | 1984 | 1896 | 2 |
| highlights | 260 | 0 | 40 | 1887 | 1556 | 1 |

`contains " … "` counts the separator, which is NOT the same as counting Exa
joins — a source page's own ellipsis survives cleaning and looks identical.
**The text arm is the false-positive floor**: it requests no highlights, so it
structurally cannot contain a backend join, and whatever it reports is the
detector reading source punctuation. Subtract that floor before reading the
highlights row as evidence of multi-passage joining.

## Snippet utility (blind judge, 0-2)

2 = carries a fact that answers the query · 1 = on-topic context only ·
0 = nav text, boilerplate, or fragments. The judge never sees which arm a
snippet came from. `unscored` counts absent snippets and judge errors and is
**not** folded into 0 — a gap is not the same finding as a useless excerpt.

| arm | mean score | share scoring 2 | share scoring 0 | unscored | n |
|---|---:|---:|---:|---:|---:|
| text | 1.75 | 78% | 3% | 0 | 130 |
| highlights | 1.89 | 89% | 0% | 0 | 130 |

### By page shape

Bucketed by the prior in `cases.ts`. The point of this table is the
`highlights-hostile` and `text-favoured` rows: a mean that only improves on
`highlights-favoured` cases is a narrower result than the headline suggests.

| page shape | cases | text mean | highlights mean | Δ |
|---|---:|---:|---:|---:|
| highlights-favoured | 12 | 1.70 | 1.92 | +0.22 |
| text-favoured | 3 | 1.87 | 2.00 | +0.13 |
| highlights-hostile | 4 | 1.45 | 1.65 | +0.20 |
| neutral | 7 | 1.97 | 1.94 | -0.03 |

## Answer quality (pairwise, both orders must agree)

This is the arm upstream shipped no eval for. Each pair is judged twice with
the sides swapped; a winner counts only when both orders agree.
`no-consensus` means the verdict flipped with position — reported, not
resolved by picking one run.

| verdict | cases |
|---|---:|
| highlights | 7 |
| text | 2 |
| tie | 14 |
| no-consensus | 3 |
| unscored | 0 |

Decided cases: 9/26. Read the split above against that denominator, not against the case count.

### Per case

| case | shape | answer winner | note |
|---|---|---|---|
| `nvda-datacenter-rev` | highlights-favoured | tie | both orders agreed: tie |
| `tsla-deliveries` | highlights-favoured | tie | both orders agreed: tie |
| `msft-azure-growth` | highlights-favoured | highlights | both orders agreed: highlights |
| `costco-comparable-sales` | highlights-favoured | text | both orders agreed: text |
| `amzn-aws-operating-income` | highlights-favoured | tie | both orders agreed: tie |
| `us-cpi-latest` | highlights-favoured | no-consensus | order-dependent: tie then text |
| `fed-funds-rate` | neutral | tie | both orders agreed: tie |
| `uk-unemployment` | highlights-favoured | tie | both orders agreed: tie |
| `eurozone-gdp-growth` | highlights-favoured | tie | both orders agreed: tie |
| `what-is-sharpe-ratio` | text-favoured | highlights | both orders agreed: highlights |
| `define-basis-point` | text-favoured | highlights | both orders agreed: highlights |
| `how-etf-works` | text-favoured | tie | both orders agreed: tie |
| `earnings-live-updates` | highlights-hostile | text | both orders agreed: text |
| `biggest-movers-today` | highlights-hostile | highlights | both orders agreed: highlights |
| `ipo-calendar` | highlights-hostile | highlights | both orders agreed: highlights |
| `crypto-prices-now` | highlights-hostile | tie | both orders agreed: tie |
| `opec-decision` | neutral | no-consensus | order-dependent: tie then highlights |
| `fed-meeting-outcome` | neutral | tie | both orders agreed: tie |
| `chip-export-controls` | neutral | no-consensus | order-dependent: highlights then tie |
| `vague-market-outlook` | neutral | highlights | both orders agreed: highlights |
| `vague-ai-spending` | neutral | tie | both orders agreed: tie |
| `cruise-occupancy` | highlights-favoured | tie | both orders agreed: tie |
| `boeing-backlog` | highlights-favoured | highlights | both orders agreed: highlights |
| `shopify-gmv` | highlights-favoured | tie | both orders agreed: tie |
| `sap-cloud-backlog` | highlights-favoured | tie | both orders agreed: tie |
| `homograph-apple-revenue` | neutral | tie | both orders agreed: tie |

