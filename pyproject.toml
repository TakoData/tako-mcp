[project]
name = "tako-mcp"
version = "0.1.0"
description = "Tako MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]==1.12.3",
    "pydantic>=2.12.0",
    "starlette>=0.48.0",
    "tako-sdk>=0.1.39",
    "uvicorn>=0.37.0",
]
